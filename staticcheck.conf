# First-pass gate: keep the default Staticcheck set while deferring the
# unused-code sweep (U1000). That sweep spans intentional test scaffolding and
# leftover helpers across batch/cmd and is tracked separately from the lint gate.
checks = ["inherit", "-U1000"]
